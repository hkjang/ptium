# Ptium v1.32.1

1.32.0을 확인하다가(원문을 두 번 적용해도 글자수 그대로 · 중복 없음) 실제 파일에서
**아무 말도 하지 않는 요점**을 봤습니다.

```
- **•**
- •
```

원본 디자인이 각 줄 옆에 그려 둔 **불릿 기호**가, 텍스트로는 자기 문단이라서 요점으로
넘어온 것입니다.

## 기호만 있는 줄은 요점이 아닙니다

글자 없이 **기호와 공백뿐인 줄**은 가져오지 않습니다(`• ▪ · ‣ ※ - – — * _ ~`).
글자가 하나라도 있으면 그대로 옵니다 — `• 핵심 요구사항`, `-30% 절감`, `A-B 테스트`,
`**중요**`는 전부 남습니다.

```
AI 플랫폼 POSTGRESQL 도입 방안.pptx   요점 48 → 45 (아무 말 없는 3개만 빠짐)
PostgreSQL Feasibility Review.pptx  요점 54 → 52
```

## 검사

기호만 있는 열 가지는 걸러지고, 글자가 섞인 여섯 가지는 남는지 고정했습니다.
실제 파일 2개로 확인했습니다 — defect 0.

전체 Go 테스트 · REST 590개 · deep 0 failures.

## 설치

```bash
gzip -dc ptium-1.32.1.tar.gz | docker load
```
